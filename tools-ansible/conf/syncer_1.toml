# Syncer Configuration.

log-level = "info"
log-file = "./log/syncer_1.log"
log-rotate = "day"

server-id = 101

meta = "./meta/syncer_1.meta"
worker-count = 32
batch = 100
flavor = "mysql"

status-addr = ":10081"

# max-retry is used for retry when network interruption.
max-retry = 100

# Notice: skip-sqls is abandoned, please use skip-ddls instead.
# skip-ddls skip ddl statements which supports regular expression. When there are DDL sqls incompatible with tidb, you should skip them via these rules.
# skip-ddls = ["^CREATE\\s+USER"]

# Notice: skip-events is abandoned, please use skip-dmls instead
# skip-dmls skip DML statements. type value can be 'insert', 'update', 'delete'.
# skip delete statements in foo.bar table.
# [[skip-dmls]]
# db-name = "foo"
# tbl-name = "bar"
# type = "delete"
# 
# skip all delete statements.
# [[skip-dmls]]
# type = "delete"
# 
# skip all delete statements in all foo.* tables.
# [[skip-dmls]]
# db-name = "foo"
# type = "delete"


## replicate-do-db has priority over replicate-do-table if have same db name
## and we support regular expression , start with '~' declare use regular expression.
#
#replicate-do-db = ["~^b.*","s1"]
#[[replicate-do-table]]
#db-name = "test"
#tbl-name = "log"

#[[replicate-do-table]]
#db-name = "test"
#tbl-name = "~^a.*"

## replicate-ignore-db has priority over replicate-ignore-table if have same db name
## and we support regular expression , start with '~' declare use regular expression.
#
#replicate-ignore-db = ["~^b.*","s1"]
#[[replicate-ignore-table]]
#db-name = "your_db"
#tbl-name = "your_table"

#[[replicate-ignore-table]]
#db-name ="test"
#tbl-name = "~^a.*"


#[[route-rules]]
#pattern-schema = "route_*"
#pattern-table = "abc_*"
#target-schema = "route"
#target-table = "abc"

#[[route-rules]]
#pattern-schema = "route_*"
#pattern-table = "xyz_*"
#target-schema = "route"
#target-table = "xyz"


[from]
host = "127.0.0.1"
user = "root"
password = "Pingcap1@#"
port = 3306

[to]
host = "127.0.0.1"
user = "root"
password = ""
port = 4000
